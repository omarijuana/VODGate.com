---
import { landing } from '../data/landing.js';

const { floatingWhatsApp } = landing;
---

<a class="floating-whatsapp" href={floatingWhatsApp.href} target="_blank" rel="noopener" aria-label={floatingWhatsApp.ariaLabel}>{floatingWhatsApp.label}</a>
